#include "main.h"
#include <stdlib.h>

/**
* _abs - Prints the absolute value of an integer
* @n: the value of the integer is accpeted
*
*  Return: ALways 0
*/
int _abs(int n)
{
	int absolute = abs(n);

	return (absolute);
}
