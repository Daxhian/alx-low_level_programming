#include "main.h"
#include <stdio.h>

/**
* add - adds two arguments
* @n1: first argument is accepted
* @n2: second argument is received
*
* Return: the sum of both arguments (integers)
*/
int add(int n1, int n2)
{
	int n1;
	int n2;
	int sum = n1 + n2;

	return (sum);
}
