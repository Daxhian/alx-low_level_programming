#include "main.h"
#include <string.h>

/**
 * _strlen - Returns the length of a string
 * @s: accepts character
 *
 * Return: length of the string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
